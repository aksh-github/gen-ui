version: "3.8"

services:
  chat-app:
    build: .
    container_name: ai-chat
    # Bind only to localhost (127.0.0.1) so port 5050 isn't open to the public internet
    ports:
      - "127.0.0.1:5050:5050"
    env_file:
      - .env
    restart: unless-stopped
